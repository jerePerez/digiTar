[build]
  # Se ejecuta desde la raíz del repo (platanoWeb/)
  command = "cd pages/sofia-invitacion && npm install && npm run build && rm -f index.html && cp dist/index.html . && rm -rf assets && cp -r dist/assets ."
  publish = "."

[build.environment]
  NODE_VERSION = "20"